import XCTest
@testable import DataServiceProtocols

final class DataServiceProtocolsTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        // XCTAssertEqual(DataServiceProtocols().text, "Hello, World!")
    }
}
